# Discord — https://discord.com/developers/applications
DISCORD_TOKEN=
DISCORD_CLIENT_ID=
# Optional: limit slash commands to one guild while developing (faster updates)
DISCORD_GUILD_ID=

# Riot Games — https://developer.riotgames.com
RIOT_API_KEY=

# Default platform alias (lan, las, na, euw, ...). Overridable per command.
DEFAULT_REGION=lan

# Default bot language: es | en (servers can override with /language)
DEFAULT_LANGUAGE=es
